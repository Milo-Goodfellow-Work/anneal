{
  "name": "Rust-Lean-LLM Dev",
  "image": "rust-lean-llm-dev",
  "workspaceFolder": "/workspace",
  "mounts": [
    "source=${localWorkspaceFolder},target=/mnt/source,type=bind,readonly",
    "source=anneal-workspace,target=/workspace,type=volume"
  ],
  "postCreateCommand": "bash -c 'if [ ! -d /workspace/.git ]; then echo \"Cloning from /mnt/source...\"; cp -a /mnt/source/. /workspace/; else echo \"Workspace already initialized\"; fi'",
  "customizations": {
    "vscode": {
      "extensions": [
        "leanprover.lean4",
        "ms-python.python",
        "google.geminicodeassist"
      ]
    }
  }
}