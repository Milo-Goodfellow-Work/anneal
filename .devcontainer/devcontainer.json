{
  "name": "Rust-Lean-LLM Dev",
  "image": "rust-lean-llm-dev",
  "customizations": {
    "vscode": {
      "extensions": [
        "leanprover.lean4",
        "ms-python.python",
        "google.geminicodeassist"
      ]
    }
  },
  "postCreateCommand": "bash -c 'mkdir -p /root/spec-lake; if [ ! -d /root/spec-lake/build ]; then echo \"Copying pre-baked cache from /opt/spec/.lake...\"; cp -r /opt/spec/.lake/* /root/spec-lake/ 2>/dev/null || true; fi; rm -rf /workspaces/anneal/spec/.lake 2>/dev/null; ln -sf /root/spec-lake /workspaces/anneal/spec/.lake; echo \"Spec .lake symlinked to /root/spec-lake for fast I/O\"'"
}