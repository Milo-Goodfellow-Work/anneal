{
  "name": "Rust-Lean-LLM Dev",
  "image": "rust-lean-llm-dev",
  "customizations": {
    "vscode": {
      "extensions": [
        "leanprover.lean4",
        "ms-python.python",
        "google.geminicodeassist"
      ]
    }
  },
  "postCreateCommand": "bash -c 'rm -rf /workspaces/anneal/spec/.lake 2>/dev/null; ln -sf /opt/spec/.lake /workspaces/anneal/spec/.lake; echo \"Spec .lake symlinked to /opt/spec/.lake for fast native I/O\"'"
}